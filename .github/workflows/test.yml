name: test
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: macos-12
    steps:
      - uses: actions/checkout@v3.0.2
      - name: Run the mkmacvm script
        run: "$GITHUB_WORKSPACE/mkmacvm"
